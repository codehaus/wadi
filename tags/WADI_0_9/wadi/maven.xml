<project default="java:jar" xmlns:j="jelly:core" xmlns:ant="jelly:ant" xmlns:aspectj="jelly:aspectj">

  <!-- aspectj -->
  <preGoal name="java:compile">
    <attainGoal name="aspectj:compile"/>
  </preGoal>







  <goal name="default" prereqs="clean, test"/>
  <goal name="dc" prereqs="clean, site:deploy, jar:deploy-snapshot"/>

  <preGoal name="site:deploy">
<!--
    <attainGoal name="javadoc"/>
    <attainGoal name="java:compile"/>
    <attainGoal name="clover"/>
-->
  </preGoal>

  <goal name="maven-linkcheck-plugin"/>
  <goal name="maven-linkcheck-plugin:register"/>
  <goal name="maven-linkcheck-plugin:report-real"/>

  <!--
  <goal name="doc">
  <attainGoal name="html2xdoc"/>
  <attainGoal name="xdoc"/>
  </goal>
  -->

<!--
  <goal name="my-aspectj:init">
    <ant:taskdef resource="org/aspectj/tools/ant/taskdefs/aspectjTaskdefs.properties">
      <ant:classpath>
        <ant:pathelement path="${plugin.getDependencyPath('aspectj:aspectjtools')}"/>
      </ant:classpath>
    </ant:taskdef>
  </goal>
-->

  <!-- ======================================== -->

<!--
  <preGoal name="java:compile">
    <attainGoal name="my-aspectj:java:compile"/>
  </preGoal>

  <goal name="my-aspectj:java:compile" prereqs="my-aspectj:init">
    <ant:echo>Compiling and Weaving to ${maven.build.dest}</ant:echo>
    <ant:iajc
      destdir="${maven.build.dest}"
      debug="${maven.compile.debug}"
      verbose="${maven.compile.verbose}"
      time="true"
      source="1.4"
      >
      <ant:forkclasspath>
	<ant:pathelement path="${plugin.getDependencyPath('aspectj:aspectjtools')}"/>
	<ant:pathelement path="${plugin.getDependencyPath('aspectj:aspectjrt')}"/>
      </ant:forkclasspath>
      <ant:classpath>
	<ant:path refid="maven.dependency.classpath"/>
      </ant:classpath>
      <ant:sourceroots>
	<ant:pathelement path="${pom.build.sourceDirectory}"/>
	<ant:pathelement path="${pom.build.aspectSourceDirectory}"/>
      </ant:sourceroots>
    </ant:iajc>
  </goal>
-->
  <!-- ======================================== -->

<!--
  <preGoal name="test:compile">
    <attainGoal name="my-aspectj:test:compile"/>
  </preGoal>

  <goal name="my-aspectj:test:compile" prereqs="my-aspectj:init">
    <ant:echo>Compiling and Weaving to ${maven.build.dest}</ant:echo>
    <ant:iajc
      destdir="${maven.test.dest}"
      debug="${maven.compile.debug}"
      verbose="${maven.compile.verbose}"
      time="true"
      source="1.4"
      >
      <ant:forkclasspath>
	<ant:pathelement path="${plugin.getDependencyPath('aspectj:aspectjtools')}"/>
	<ant:pathelement path="${plugin.getDependencyPath('aspectj:aspectjrt')}"/>
      </ant:forkclasspath>
      <ant:classpath>
	<pathelement path="${maven.test.dest}"/>
	<pathelement path="${maven.build.dest}"/>
	<path refid="maven.dependency.classpath"/>
	<pathelement path="${plugin.getDependencyPath('junit')}"/>
      </ant:classpath>
      <ant:sourceroots>
	<ant:path refid="maven.test.compile.src.set"/>
      </ant:sourceroots>
    </ant:iajc>
  </goal>
-->

  <!-- ======================================== -->

</project>
